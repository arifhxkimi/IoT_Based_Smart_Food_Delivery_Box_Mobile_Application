<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.SmartFoodDeliveryBox" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- dark theme config -->
    </style>

    <!-- ✅ Splash Theme (dark) -->
    <style name="Theme.SmartFoodDeliveryBox.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.SmartFoodDeliveryBox</item>
    </style>

</resources>
